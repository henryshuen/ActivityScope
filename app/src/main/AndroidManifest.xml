<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myactivitylauncher">

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyActivityLauncher">

        <!-- 啟動頁：列出安裝套件 -->
        <activity
            android:name=".InstalledPackagesActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyActivityLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 第二頁：顯示某個 app 的 activities -->
        <activity
            android:name=".ActivitiesActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyActivityLauncher" />

        <!-- 第三頁：最近啟動紀錄 -->
        <activity
            android:name=".RecentActivitiesActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyActivityLauncher" />

        <activity
            android:name=".ActivityDetailActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyActivityLauncher" />

        <activity
            android:name=".FavoriteActivitiesActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyActivityLauncher" />

        <activity android:name=".SettingsActivity" />




    </application>


</manifest>